import {Flex} from "../../styles/common";


const Loader = ({number}) => {
    return (
        <Flex jcontent={'center'}>
            <span className="loader"></span>
        </Flex>
    );
}

export default Loader
